# 대화 분석 — pyannote Community-1 로컬 연결

상태: **연결·모델 배치·실제 실행 1회까지 끝났다.**
고를 수 있는 경로로 붙였고 **기본은 기존 엔진**이다. master 병합 안 함.

## 모델 배치 (2026-09-15 완료)

사용자가 이용 조건을 수락하고 파일을 내려받아 주었다. 받은 파일을 **공식 저장소의 sha256 과
대조해** 자리에 복사했다(이름이 아니라 내용으로 판별했다 — 같은 이름이 겹쳐 `(1)` 로 저장된
것이 있었다). **원본은 그대로 두었다.**

    E:\...\AudioForge\externals\diarization_models\community-1\
    ├─ config.yaml                     444 B
    ├─ embedding/pytorch_model.bin      26,646,242 B   sha 6f10ff60898a1d18
    ├─ segmentation/pytorch_model.bin    5,906,507 B   sha 7ad24338d844fb95
    └─ plda/
       ├─ plda.npz                        133,852 B   sha 9b77bcd840692710
       └─ xvec_transform.npz              134,376 B   sha 325f1ce8e48f7e55

다섯 개 모두 공식 값과 크기·해시가 같다. HTML 오류 페이지·미완료 파일 없음.

`config.yaml` 은 `$model/segmentation`, `$model/embedding`, `$model/plda` 를 가리킨다.
설치된 로더(`pyannote/audio/core/pipeline.py`)가 이 표기를 **모델 폴더 하위로 풀어 주는 것**을
코드에서 확인했고, 실제 적재로도 확인했다 — 외부를 다시 찾지 않는다.

## 설치한 것 (이 장비에서 실제로 돈 것)

앱 파이썬은 ComfyUI 내장 파이썬이라 섞지 않았다. **전용 환경**을 따로 만들었다.

    externals/diarization_venv/                  ← 격리 실행 환경
    externals/diarization_models/community-1/    ← 가중치(배치 완료)

- `pyannote.audio` **4.0.7** · torch **2.11.0+cu128** · torchaudio 2.11.0+cu128 ·
  torchcodec 0.16.0 · soundfile 0.14.0
- GPU 인식 확인: **RTX 5070 Ti · sm_120 · CUDA 12.8** (torch 가 `is_available()=True`).
- **이용 조건**: 코드(pyannote.audio)는 공개 배포판을 그대로 설치했다. **가중치는 게이트**라
  사용자가 수락해야 받을 수 있다 — 위 참조.

## 밖으로 나가는 길을 막은 자리

- 텔레메트리: `PYANNOTE_METRICS_ENABLED=false` (설치본 코드에서 이 환경변수가 스위치임을 확인).
- 허브 통신: `HF_HUB_OFFLINE=1`, `HF_HUB_DISABLE_TELEMETRY=1` — 실행 중 다운로드를 막는다.
- **클라우드 SDK(pyannoteai)는 쓰지 않는다.** Precision-2 API 로 대체하지 않는다.
- 다리가 받는 것은 로컬 파일 경로뿐이고, 내보내는 것은 로컬 JSON 하나다.

## 연결한 모양

    대화 모드 > 분석 방식 : [기본] [Community-1]      ← 기본은 '기본'(기존 엔진)

- 화자 수 설정(2~5명)을 `num_speakers` 로 그대로 전달한다.
- 결과 두 가지를 **구분해** 쓴다.
  · `exclusive_speaker_diarization` → 화자별 트랙 배정과 구간 수정에 쓴다(한 시점에 한 화자).
  · `speaker_diarization` 에서 계산한 **겹침 구간**은 따로 보존해 화면에 별도로 표시한다.
- ★**겹침을 찾았다는 것은 겹친 목소리를 갈라냈다는 뜻이 아니다.** 화면에도 그렇게 적었다.
- **분석은 16kHz 모노**로 하지만 **출력 트랙은 원본 시간축·음질 기준**으로 만든다 —
  기존 `dialogue_rebuild` 가 원본에서 그 구간을 그대로 떠 온다(경계 페이드도 기존 규칙).
- 구간 수정 화면·저장 형식·재구성은 **이미 있는 것을 그대로** 쓴다. 새로 만들지 않았다.
- 진행·오류는 기존 작업 흐름으로 흐른다. 준비 안 됨은 `DIARIZE_NOT_READY` 로,
  실행 실패는 사유와 함께 올린다.

## 실패를 숨기지 않는다

- 격리 환경 없음 → `DIARIZE_VENV_MISSING`
- 모델 없음 → `DIARIZE_MODEL_MISSING` (무엇을 해야 하는지 문장으로 함께 알린다)
- 다리 실패 → `DIARIZE_FAILED(exit N): <사유>`
- GPU 를 못 쓰면 → `DIARIZE_CUDA_UNAVAILABLE`. **몰래 CPU 로 바꾸지 않는다.**
- 어느 경우에도 **기존 엔진으로 슬그머니 갈아타지 않는다**(검사로 확인).

## 확인한 것

- 계약 검사 `python/test_diarize_community1.py` (9건) — 준비 안 됨 사유, cuda 고정,
  화자 수 전달, 텔레메트리·오프라인 차단, 겹침 분리 보관, 겹침 구간 계산.
- 화면 표적 검사 `test/e2e/diarize-engine.e2e.mjs` (14건) — 대화 모드에만 선택이 있고 기본이
  기존 엔진, 다른 모드에는 없음, 고른 값 반영, **모델 없음이 사유와 함께 실패**하고 그때
  결과 파일이 만들어지지 않음, 겹침을 따로 표시하고 구간 목록에 섞지 않음.

## 이 장비에서 실제로 돈 결과 (1회)

저장소 검사 음원(한국어 18.56초, 24kHz 모노)으로 **오프라인·텔레메트리 비활성** 상태에서
한 번 돌렸다. 토큰을 다시 요구하지 않았고 받아 둔 로컬 파일만 썼다.

- **실제 장치: cuda — NVIDIA GeForce RTX 5070 Ti** (CPU 로 바뀌지 않았다)
- 모델 적재 **2.29초**, 분석 **0.79초**(제품 경로 재실행 0.82초)
- **최대 GPU 메모리 2,772 MB**
- 오류 없음. 경고 두 줄은 나왔다 — TF32 권장 안내와, 아주 짧은 구간에서 표준편차 자유도가
  0 이하라는 torch 경고(`std(): degrees of freedom is <= 0`). 실행은 정상 종료했다.
- 결과: 구간 3개(다리 단독 실행) / 4개(제품 경로, 화자 수 2 지정), 겹침 0곳.
- 제품 경로 완주 — **화자별 출력 2개**가 나왔고, 둘 다 **18.56초·24kHz** 로 원본 시간축·
  표본율을 따랐다(분석용 16kHz 가 아니다).

★**정확도는 말하지 않는다.** 이 음원에 정답 화자 표기가 없고, 기존 엔진과 같은 자료로 견준
근거도 없다. 여기서 확인한 것은 **연결이 끝까지 돈다**는 사실이다.

## 확인 방법 (갱신)

- 계약 검사 `python/test_diarize_community1.py` (9건).
- 화면 표적 검사 `test/e2e/diarize-engine.e2e.mjs` (17건) — 화면에서 시작해 **실제로 한 번
  돌려** 완주(44초, 앱 기동 포함)·구간 4개 화면 표시·화자별 출력 2개까지 확인한다.
  모델이 없는 환경에서는 사유와 함께 실패하는 쪽으로 판정한다.

## 아직 확인하지 않은 것

- **여러 사람이 실제로 대화하는 음원으로는 돌리지 않았다.** 쓴 음원은 검사용 한 사람 발화라
  화자 구분 품질을 말할 수 없다(구간 수는 결과일 뿐 정확도가 아니다).
- 기존 엔진과의 정확도 비교 — 직접 비교 근거가 없어 하지 않는다.
- 새 엔진을 기본값으로 바꿀지는 **별도 판단**으로 남긴다. 지금 기본은 기존 엔진이다.
